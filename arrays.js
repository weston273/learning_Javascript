//🧠 Concept:

//Store multiple items in one variable.

//Syntax:

let fruits= ["apple","banana","mango"];

console.log(fruits[0]); // showing apple coz of indexing, first number is noted by0 then 1 and so on;
fruits.push("orange"); // adding orange to the array (last place, kwekupedzisira)
fruits.unshift("kiwi"); // adding kiwi to the array (kwekutanga)
fruits.pop(); // removing orange from the array
fruits.shift(); // removing kiwi from the array
console.log(fruits);

//✔ Task

let numbers = [1,2,3,4,5]

// to print the third number

console.log(numbers[2]); // since 2 means position 3


// to add 6 to the end

numbers.push(6);
console.log(numbers);
