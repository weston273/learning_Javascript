// Boolean Logic

// You have two variables

let hasID = true;
let hasTicket = false;

// write a condition that logs:
//"Allowed entry if both are true"
//"Missing ID or ticket if either is false"

if (hasID = true && hasTicket == true) {
    console.log("You can enter")
} else {
    console.log("You need an ID or a ticket")
}