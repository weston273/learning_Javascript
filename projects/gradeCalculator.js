// A simple login system

// input => Marks for 5 subjects
// Output: Average + Grade (A,B,C,D, Fail)
// Bonus: Reject invalid inputs (like letters or out-of-range numbers)

const readline = require("readline").createInterface({
    input: process.stdin,
    output: process.stdout,
});

let i = 1
i<=5

    readline.question("Enter Marks: ", function(marks) {
    
    


        readline.close();
    })