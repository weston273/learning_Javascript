// = assignment operator
// == comparison operator (compare if value are equal)
// === strict equality operator (compares if values & datatpes are equal)
// != inequality operator
// !== strict inequality operator

const PI = 3.14;

if(PI !== '3.14'){
    console.log('That is not PI')
} else {
    console.log('That is PI')
}